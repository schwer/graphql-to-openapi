type Address {
  addr1: String!
  addr2: String
  city: String!
  state: String!
  zip: String!
  country: String!
}

type Person {
  id: ID!
  firstName: String!
  lastName: String!
  address: Address!
}

type Query {
  person(id: ID!): Person!
}
